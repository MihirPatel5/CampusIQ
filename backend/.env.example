# Django Settings
DEBUG=True
SECRET_KEY=i@oja*tc5u(gl4o7^2g8lexeoj0ho!1i+v)o)z1lr-5s^u^_jg
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
# For development with SQLite (default)
USE_SQLITE=True

# For production with PostgreSQL (set USE_SQLITE=False)
# DATABASE_NAME=school_erp_db
# DATABASE_USER=school_user
# DATABASE_PASSWORD=your-secure-password-here
# DATABASE_HOST=localhost
# DATABASE_PORT=5432

# JWT Settings (in minutes for access, days for refresh)
JWT_ACCESS_TOKEN_LIFETIME=15
JWT_REFRESH_TOKEN_LIFETIME=7

# CORS Configuration (comma-separated origins)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# File Storage
MEDIA_ROOT=media
STATIC_ROOT=staticfiles
